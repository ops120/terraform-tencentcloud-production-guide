# Phase 1：基础入门

> 预计用时：2~3 天 
> 目标：理解 IaC 理念，掌握 Terraform 核心概念与基本操作
---

## 1.1 基础设施即代码(IaC) 概述

### 为什么需要 IaC？
传统手动管理云资源的痛点：
- 🔴 **控制台点点点，容易出错、不可重复** — 每次操作都是手动点击，无法保证两次操作完全一致
- 🔴 **环境差异导致"在我机器上能跑"** — 开发、测试、生产环境配置不一致，定位问题极其困难
- 🔴 **无法版本控制基础设施** — 某个安全规则是谁改的？什么时候改的？无从追溯
- 🔴 **团队协作困难** — 多人同时操作控制台，互相覆盖配置，缺乏协调机制
- 🔴 **灾难恢复慢** — 整个环境被误删，重建需要数天甚至数周
IaC 的核心优势：
- ✅ **可重复性**：同一份代码，创建完全一致的环境（dev / staging / prod 完全一致）
- ✅ **版本控制**：基础设施代码化，像应用代码一样 Git 管理，支持回滚、分支、Code Review
- ✅ **可审计**：每一次变更都有记录（谁、什么时间、改了哪些资源）
- ✅ **自动化**：无人值守部署和销毁，CI/CD 流水线自动执行
- ✅ **文档即代码**：代码本身就是最准确的文档，不会过时

### Terraform 的定位
Terraform 是 **声明式** 的基础设施编排工具，由 HashiCorp 公司开发：

- **声明式**：你描述"想要什么"（`cidr_block = "10.0.0.0/16"`），Terraform 决定"怎么做"（调用 API 创建 VPC、路由表等）
- **Provider 架构**：一个工具管理所有云（腾讯云、AWS、阿里云、Azure…），学习一次，到处使用
- **不可变基础设施**：修改资源时，倾向重建而非原地修改，确保环境一致性
> 💡 **Terraform vs 其他工具**
> - **Ansible/SaltStack**：配置管理工具，关注"在服务器上安装什么软件"
> - **Terraform**：基础设施编排工具，关注"创建什么云资源"
> - **Pulumi**：类似 Terraform，但使用通用编程语言（TypeScript/Python/Go）
> - **CloudFormation**：仅限 AWS，Terraform 是云厂商中立的
---

## 1.2 安装 Terraform

### Windows

```powershell
# 方式一：Chocolatey（推荐）
choco install terraform

# 方式二：Scoop
scoop install terraform

# 方式三：手动下载
# 1. 访问 https://developer.hashicorp.com/terraform/install
# 2. 下载 Windows amd64 版本
# 3. 解压后将 terraform.exe 放到 C:\Windows\System32 或加入 PATH
```

### macOS

```bash
brew tap hashicorp/tap
brew install hashicorp/tap/terraform
```

### Linux

```bash
wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor | sudo tee /usr/share/keyrings/hashicorp-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/hashicorp.list
sudo apt update && sudo apt install terraform
```

### 验证安装

```bash
terraform version
# 输出示例：Terraform v1.9.x (1.6+ 即可，推荐使用最新稳定版)
```

> ⚠️ **常见陷阱**：安装后如果提示 `terraform 不是内部或外部命令`，说明 terraform.exe 所在的目录不在 PATH 环境变量中。将安装目录添加到 PATH 后重启终端即可。
---

## 1.3 HCL 语法速通
Terraform 使用 **HCL**（HashiCorp Configuration Language）编写配置文件，扩展名为 `.tf`。
### 1.3.1 基本语法结构

HCL 的核心结构是 **块（Block）** 和 **参数（Argument）**：
```hcl
# 块类型 "块标签" {
#   参数名 = 参数值
# }

resource "tencentcloud_vpc" "my_vpc" {
  name       = "my-vpc"          # 参数：字符串值
  cidr_block = "10.0.0.0/16"     # 参数：字符串值
  is_multicast = false            # 参数：布尔值
  tags = {                        # 参数：map 值
    Name = "my-vpc"
  }
}
```

**语法规则：**
1. **块类型**（Block Type）：`resource`、`data`、`provider`、`variable`、`output`、`terraform` 等
2. **块标签**（Block Label）：块的具体名称，数量因块类型而异（`resource` 需要 2 个标签：类型和名称）
3. **参数**（Argument）：`参数名 = 参数值` 的键值对
4. **标识符**（Identifier）：资源名、变量名等，由字母、数字、下划线组成，不能以数字开头
5. **注释**（Comment）：`#` 单行注释 或 `/* ... */` 多行注释

### 1.3.2 注释

```hcl
# 这是单行注释

/*
  这是
  多行注释
*/

// 这也是单行注释（HCL 支持）
```

### 1.3.3 表达式（Expression）
HCL 支持丰富的表达式，用于动态计算值：

```hcl
# 字符串插值
name = "web-server-${count.index + 1}"

# 条件表达式
instance_type = var.env == "prod" ? "S5.4XLARGE32" : "S5.LARGE8"

# 算术运算
port = 80 + 443  # 不常用，但支持

# 比较运算
enabled = var.count > 0

# 逻辑运算
both = var.a && var.b
either = var.a || var.b
negated = !var.a
```

### 1.3.4 常见数据类型

```hcl
# 字符串
name = "production"

# 数字
port = 443

# 布尔值
enabled = true

# null（空值，表示"未设置"）
override_sg = null

# 列表（List）
availability_zones = ["ap-guangzhou-3", "ap-guangzhou-4"]

# 列表访问
first_az = availability_zones[0]  # "ap-guangzhou-3"

# Map（字典）
tags = {
  Name        = "web-server"
  Environment = "production"
}

# Map 访问
env = tags["Environment"]  # "production"
# 或 env = tags.Environment  # "production"（key 是字母数字时可用）

# 对象（Object）— 结构体
instance_config = {
  name   = "web"
  type   = "S5.LARGE8"
  count  = 2
  tags   = {}
}

# 元组（Tuple）— 不同类型元素的列表
mixed = ["string", 42, true]
```

### 1.3.5 引用语法

Terraform 通过引用（Reference）来访问资源的属性和变量的值：

```hcl
# 引用资源属性：<资源类型>.<资源名称>.<属性名>
resource "tencentcloud_cvm_instance" "web" {
  vpc_id = tencentcloud_vpc.my_vpc.id  # 引用 VPC 的 ID
}

# 引用变量：var.<变量名>
subnet_id = var.subnet_id

# 引用本地值：local.<本地值名>
name = local.instance_name

# 引用 Data Source：data.<数据源类型>.<名称>.<属性>
image_id = data.tencentcloud_images.default.images[0].image_id

# 引用模块输出：module.<模块名>.<输出名>
vpc_id = module.vpc.vpc_id
```

### 1.3.6 字符串插值（String Interpolation）
```hcl
# 在字符串中嵌入表达式
instance_name = "web-server-${count.index + 1}"

# 调用函数
description = "Created at ${timestamp()}"

# 复杂表达式
name = "${var.project}-${var.env}-${var.role}"
```

### 1.3.7 块嵌套（Nested Block）
```hcl
resource "tencentcloud_cvm_instance" "web" {
  instance_name = "web-server"
  
  # 嵌套块（重复块）
  data_disks {
    data_disk_type = "CLOUD_SSD"
    data_disk_size = 100
  }
  
  data_disks {
    data_disk_type = "CLOUD_PREMIUM"
    data_disk_size = 200
  }
}
```

> 💡 **HCL vs JSON**：Terraform 也支持 `.tf.json` 格式，但日常使用 `.tf`（HCL 格式）更简洁易读。JSON 格式主要用于机器生成。
---

## 1.4 核心命令

### 工作流
```
init → plan → apply → destroy
```

这是 Terraform 的"黄金工作流"，每一次基础设施变更都遵循这个流程。
### 1.4.1 terraform init — 初始化（最重要的一步）

```bash
terraform init
```

`terraform init` 是 **每个 Terraform 项目第一个执行的命令**，它完成以下工作：
1. **下载 Provider 插件**：根据 `required_providers` 配置，从 Terraform Registry 下载对应版本的 Provider 二进制文件到 `.terraform/providers/` 目录
2. **初始化后端存储（Backend）**：配置 State 文件的存储位置（本地或远程如 COS）
3. **下载模块**：如果使用了远程模块（如 Git 仓库中的模块），会下载到 `.terraform/modules/`
4. **生成锁定文件**：创建 `.terraform.lock.hcl` 记录 Provider 版本信息

```bash
# 初始化时指定后端配置
terraform init -backend-config="bucket=my-terraform-state"

# 重新初始化（升级 Provider 或修改后端配置后）
terraform init -reconfigure

# 升级 Provider 到最新允许版本
terraform init -upgrade

# 只下载 Provider，不初始化后端
terraform init -backend=false
```

> ⚠️ **常见陷阱**：每次 clone 项目后，**必须**先执行 `terraform init`。不要将 `.terraform/` 目录提交到 Git，它包含本机特定的二进制文件。
### 1.4.2 terraform plan — 预览变更

```bash
terraform plan
```

`terraform plan` 比较 **当前代码** 和 **当前 State**，计算出需要执行的变更：
**输出符号含义：**

| 符号 | 含义 | 说明 |
|------|------|------|
| `+` | 创建 (create) | 资源在代码中但不在 State 中，将创建 |
| `-` | 删除 (delete) | 资源在 State 中但不在代码中，将删除 |
| `~` | 修改 (update in-place) | 资源的属性发生变化，原地修改 |
| `-/+` | 销毁重建 (replace) | 属性不可原地修改，需先销毁再创建 |
| `<=` | 读取 (read) | Data Source 读取已有数据 |

```bash
# 输出到文件（用于后续 apply）
terraform plan -out=plan.out

# 指定变量文件
terraform plan -var-file=prod.tfvars

# 限制只查看某些资源的变更
terraform plan -target=tencentcloud_cvm_instance.web

# 不显示不需要变更的资源
terraform plan -compact-warnings
```

> 💡 **最佳实践**：在 CI/CD 中执行 `terraform plan` 并将输出展示在 PR 评论中，让团队成员审查变更内容。
### 1.4.3 terraform apply — 执行变更

```bash
terraform apply
```

执行 plan 中定义的变更：
- 创建 / 修改 / 删除资源
- 默认需要输入 `yes` 确认
- 更新 `terraform.tfstate` 文件

```bash
# 使用 plan 文件（确认 apply 的内容和 plan 一致）
terraform apply plan.out

# 自动确认（生产环境慎用！）
terraform apply -auto-approve

# 只应用特定资源
terraform apply -target=tencentcloud_cvm_instance.web
```

> ⚠️ **安全警告**：`-auto-approve` 跳过了确认步骤，在 CI/CD 中使用时务必确保 plan 已经过审查。
### 1.4.4 terraform destroy — 销毁资源
```bash
terraform destroy
```

销毁所有被管理的资源。**这是一个危险操作**，默认需要输入 `yes` 确认。
```bash
# 自动确认
terraform destroy -auto-approve

# 只销毁特定资源
terraform destroy -target=tencentcloud_cvm_instance.web
```

> ⚠️ **重要提示**：`terraform destroy` 会销毁 **State 中所有资源**。如果你只想删除特定资源，使用 `terraform destroy -target` 或 `terraform state rm` 后手动删除。
### 1.4.5 其他辅助命令

```bash
# 格式化代码（统一风格）
terraform fmt

# 递归格式化所有子目录
terraform fmt -recursive

# 验证语法正确性
terraform validate

# 查看当前 State 中的资源列表
terraform state list

# 查看 Provider 版本信息
terraform providers
```

---

## 1.5 State 状态文件
### 什么是 State？
Terraform 通过 `terraform.tfstate` 文件记录已创建的资源及其属性。它是 Terraform 的"数据库"——**Terraform 的大脑**。
```
你的 .tf 文件 → 描述"想要什么"
    → terraform apply
    → terraform.tfstate → 记录"实际有什么"
    → 下次 plan → 对比 .tf 和 .tfstate 计算差异
```

### State 存储了什么？

State 文件是一个 JSON 格式的文件，记录了：
- 每个资源的 **ID**（如 `vpc-xxxxx`、`ins-xxxxx`）
- 每个资源的 **所有属性**（IP 地址、状态、配置等）
- 资源之间的 **依赖关系**
- **Meta 数据**（Provider 版本、Terraform 版本等）

### State 的重要性
**理解 State 是理解 Terraform 的关键：**

1. **映射**：State 将 `.tf` 文件中的资源声明（`tencentcloud_vpc.main`）映射到云上的真实资源（`vpc-xxxxx`）
2. **元数据**：State 存储资源的完整属性，包括那些不在 `.tf` 文件中声明的属性（如资源的创建时间）
3. **性能**：Terraform 不需要每次都调用云 API 查询所有资源，而是直接从 State 读取
4. **同步**：多人协作时，State 是确保所有人看到同一份基础设施状态的依据

### 关键注意事项

⚠️ **State 文件包含敏感信息**（如资源 ID、IP 地址、数据库连接地址），不要提交到 Git

⚠️ **State 文件丢失 = Terraform 不认已创建的资源**（但资源本身还在云上）——这意味着 Terraform 会尝试创建全新的资源，导致资源冲突
⚠️ **多人协作必须使用远程 State**（见 Phase 4），否则会出现：
- 覆盖别人的变更
- 并发写入导致 State 损坏
- 无法锁定

### .terraform 目录和 .terraform.lock.hcl 说明

```
.terraform/                    # Provider 插件和模块缓存目录（自动生成，不归 Git）
├── providers/                 # 下载的 Provider 二进制文件
│   └── registry.terraform.io/
│       └── tencentcloudstack/
│           └── tencentcloud/
│               └── 1.81.x/
│                   └── windows_amd64/
│                       └── terraform-provider-tencentcloud.exe
├── modules/                   # 下载的远程模块
└── environment/               # 当前 workspace 信息

.terraform.lock.hcl            # Provider 版本锁定文件（应提交到 Git！）
```

**`.terraform.lock.hcl` 的作用：**
- 记录所有 Provider 的版本哈希值，确保所有团队成员使用完全相同的 Provider 版本
- 防止 Provider 被篡改（供应链安全）
- 当 `terraform init -upgrade` 时更新
> ⚠️ **重要**：`.terraform/` 目录不要提交到 Git，但 **`.terraform.lock.hcl` 应该提交到 Git**。
---

## 1.5.5 动手实践：第一个真正能跑的 Hello World 🎆

> 前面的示例都用腾讯云 Provider，但你需要先配置认证才能跑。**这个示例用 `local_file` Provider，不需要任何云账号，现在就能跑！**

### 创建项目目录

```bash
mkdir hello-terraform && cd hello-terraform
```

### 创建 main.tf

```hcl
terraform {
  required_version = ">= 1.6"
  required_providers {
    local = {
      source  = "hashicorp/local"
      version = ">= 2.4.0"
    }
  }
}

# 创建一个本地文件（不需要任何云账号！）
resource "local_file" "hello" {
  filename        = "${path.module}/hello.txt"
  content         = "Hello, Terraform! 🎉\n我的第一个 Terraform 资源！"
  file_permission = "0644"
}
```

### 跑通完整工作流

跟着我一步步执行，体验 Terraform 的完整流程：

```bash
# ① init — 初始化项目，下载 Provider
terraform init
# 输出示例：
#   Initializing the backend...
#   Initializing provider plugins...
#   - Finding hashicorp/local versions matching ">= 2.4.0"...
#   - Installing hashicorp/local v2.4.x...
#   - Installed hashicorp/local v2.4.x (signed by HashiCorp)
#   Terraform has been successfully initialized!
```

```bash
# ② plan — 预览将要执行的操作
terraform plan
# 输出示例：
#   Terraform will perform the following actions:
#
#     # local_file.hello will be created
#     + resource "local_file" "hello" {
#         + content              = "Hello, Terraform! 🎉\n我的第一个 Terraform 资源！"
#         + content_base64sha256 = (known after apply)
#         + filename             = "./hello.txt"
#         + id                   = (known after apply)
#       }
#
#   Plan: 1 to add, 0 to change, 0 to destroy.
```

> 注意 `+` 号表示"创建"——你第一次看到真实的 Terraform 计划了！🎉

```bash
# ③ apply — 执行变更，创建文件
terraform apply
# 输入 yes 确认
# 输出：
#   local_file.hello: Creating...
#   local_file.hello: Creation complete after 0s [id=xxx]
#
#   Apply complete! Resources: 1 added, 0 changed, 0 destroyed.
```

```bash
# 验证文件已创建
cat hello.txt
# 输出：Hello, Terraform! 🎉
#       我的第一个 Terraform 资源！
```

```bash
# ④ destroy — 清理资源（删除文件）
terraform destroy
# 输入 yes 确认
# 输出：Destroy complete! Resources: 1 destroyed.
```

### 你刚刚做了什么？

| 步骤 | 命令 | 实际发生了什么 |
|------|------|---------------|
| ① init | `terraform init` | 下载 `local` Provider 插件到 `.terraform/` |
| ② plan | `terraform plan` | 读取 `main.tf`，对比 State（空），列出要创建的文件 |
| ③ apply | `terraform apply` | 调用 `local` Provider 的 API，在磁盘上创建 `hello.txt`，记录到 State |
| ④ destroy | `terraform destroy` | 调用 `local` Provider 的 API，删除 `hello.txt`，清空 State |

> 💡 **这就是 Terraform 的核心**：`init` 装好工具 → `plan` 看要做什么 → `apply` 执行 → `destroy` 清理。所有云资源（VPC、CVM、数据库）都是这个流程！

### 检查你的 State 文件

```bash
# 查看 State 文件内容
cat terraform.tfstate

# 输出是一个 JSON 文件，包含：
# {
#   "version": 4,
#   "terraform_version": "1.9.x",
#   "resources": [
#     {
#       "module": "root",
#       "mode": "managed",
#       "type": "local_file",
#       "name": "hello",
#       "provider": "provider[\"registry.terraform.io/hashicorp/local\"]",
#       "instances": [
#         {
#           "attributes": {
#             "content": "Hello, Terraform! 🎉\n我的第一个 Terraform 资源！",
#             "filename": "./hello.txt",
#             "id": "..."
#           }
#         }
#       ]
#     }
#   ]
# }
```

这就是 Terraform 的"大脑"——它记录了资源的所有属性。Phase 2 开始，这个文件会记录你的云资源。
---

## 1.6 进阶：第一个腾讯云项目结构

### 目录结构

```
hello-terraform/
├── main.tf      # 主配置文件
├── variables.tf # 变量定义
└── outputs.tf   # 输出定义
```

### main.tf

```hcl
terraform {
  required_version = ">= 1.6"
  required_providers {
    tencentcloud = {
      source  = "tencentcloudstack/tencentcloud"
      version = ">= 1.81.0"
    }
  }
}

provider "tencentcloud" {
  # 暂时用空 Provider，Phase 2 配置认证
}
```

### 初始化并验证

```bash
# 1. 初始化（下载 Provider）
terraform init

# 2. 格式化代码
terraform fmt

# 3. 验证语法
terraform validate

# 4. 查看 Provider 信息
terraform providers
```

### 和 Hello World 对比

| | 1.5.5 Hello World（你刚跑的） | 1.6 腾讯云项目（下一步） |
|------|------|------|
| Provider | `hashicorp/local` | `tencentcloudstack/tencentcloud` |
| 需要什么？| 不需要任何账号 | 需要腾讯云密钥（Phase 2 配置） |
| 创建什么？| 本地文件 | CVM、VPC 等云资源 |
| 花不花钱 | **免费！** | 会产生云资源费用 |
| 当前状态 | ✅ 你已经跑通了！| ⏳ 等 Phase 2 配置认证后运行 |

---

## 1.7 常见陷阱与最佳实践
### 常见陷阱

| 陷阱 | 后果 | 正确做法 |
|------|------|----------|
| 忘记 `terraform init` | `Error: Could not load plugin` | 每次 clone 后先 init |
| 多人使用本地 State | 覆盖、冲突、损坏 | 从第一天就用远程 State |
| 手动在控制台修改 Terraform 管理的资源 | Drift（漂移） | 永远通过 Terraform 修改 |
| 硬编码密钥在 `.tf` 文件中 | 密钥泄露 | 使用环境变量或密钥管理服务 |
| 不检查 `terraform plan` 直接 apply | 误操作删资源 | 始终先 plan 再 apply |
| 提交 `.terraform/` 目录到 Git | 二进制文件臃肿 | 添加到 `.gitignore` |

### 最佳实践原则
1. **从第一天就用远程 State** — 不要等团队大了再迁移
2. **模块化思维** — 资源超过 3 个就应该考虑模块化
3. **先 plan 再 apply** — 永远先看变更内容
4. **敏感信息不入代码** — 密钥、密码通过环境变量传入
5. **标签化管理** — 每个资源都打上标签（Project、Environment、ManagedBy）
6. **版本控制** — Provider 版本锁定（`.terraform.lock.hcl` 提交到 Git）
---

## 🎯 互动练习

### 自测题（每题 5 秒思考）

<details>
<summary>📝 第 1 题：Terraform 是声明式还是命令式工具？</summary>

**答案：声明式**。你描述"想要什么"（`cidr_block = "10.0.0.0/16"`），Terraform 决定"怎么做"（调用 API 创建 VPC）。</details>

<details>
<summary>📝 第 2 题：以下哪个命令是 Terraform 项目的第一个命令？</summary>
A) `terraform plan`  
B) `terraform init`  
C) `terraform apply`  
D) `terraform destroy`

**答案：B**。`terraform init` 下载 Provider 插件、初始化 Backend，是每个项目的第一个命令。</details>

<details>
<summary>📝 第 3 题：State 文件的作用是什么？</summary>

**答案**：State 文件记录已创建的资源及其属性，是 Terraform 的"数据库"。下次 `plan` 时对比 `.tf` 文件和 State 文件计算差异。</details>

<details>
<summary>📝 第 4 题：`.terraform/` 目录和 `.terraform.lock.hcl` 哪个应该提交到 Git？</summary>

**答案**：`.terraform.lock.hcl` 应该提交到 Git（锁定 Provider 版本），`.terraform/` 目录不应该提交（自动生成、包含二进制文件）。</details>

### 🪄 动手试一试
1. 修改 Hello World 的 `content`，改成一句你喜欢的句子，重新 `apply` 观察 `plan` 输出
2. 把 `filename` 改成 `my-first-tf.txt`，再次 `apply`，看看 Terraform 显示的是 `+` 创建还是 `~` 修改
3. 运行 `terraform state list`，看看 State 里记录了什么资源
### 💡 思考题

> 如果删除了 `hello.txt` 文件（手动删除），然后运行 `terraform plan`，会发生什么？
>
> <details>
> <summary>点击查看答案</summary>
> Terraform 会检测到资源被手动删除，plan 会显示 `+ create`，表示要重新创建。因为 State 文件里还记录着这个资源，但实际资源已经不在了（Drift 漂移）。这就是"不要手动改"的原因。
> </details>

---

## ✅ 本阶段掌握要点
- [ ] 理解 IaC 的核心价值
- [ ] 安装 Terraform 并验证版本
- [ ] 理解 HCL 基本语法（块、参数、标识符、注释、表达式）
- [ ] 理解字符串插值的用法
- [ ] 掌握 `init → plan → apply → destroy` 工作流
- [ ] ✅ **已跑通 Hello World**（`local_file` 示例）
- [ ] 理解 `terraform init` 做了什么（下载 Provider、初始化 Backend）
- [ ] 理解 `.terraform/` 目录和 `.terraform.lock.hcl` 的区别
- [ ] 理解 `+`/`-`/`~`/`-/+` 符号的含义
- [ ] 理解 State 文件的作用和重要性
- [ ] 能创建 Terraform 项目结构
- [ ] 了解常见陷阱和最佳实践
---

**下一步 → [02-腾讯云Provider入门](02-腾讯云Provider入门.md)**