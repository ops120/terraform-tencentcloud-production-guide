# Phase 6：运维与排错

> 预计用时�?~3 �? 
> 目标：掌�?Terraform 运维技能，能独立排查和解决常见问题

---

## 6.1 常见错误及解�?
### 6.1.1 Provider 错误

```bash
# 错误：Provider 未安�?Error: Could not load plugin

# 常见原因�?# 1. 没有执行 terraform init
# 2. .terraform/ 目录被删�?# 3. Provider 版本指定错误

# 解决
terraform init
```

### 6.1.2 认证错误

```bash
# 错误：认证失�?Error: TencentCloud API error: InvalidSecretId

# 常见原因�?# 1. SecretId/SecretKey 错误
# 2. 环境变量未设�?# 3. 密钥已禁用或过期

# 解决
$env:TENCENTCLOUD_SECRET_ID = "正确�?SecretId"
$env:TENCENTCLOUD_SECRET_KEY = "正确�?SecretKey"
```

### 6.1.3 资源冲突

```bash
# 错误：资源已存在
Error: Code=ResourceInUse
  ResourceInUse: 资源已存�?
# 原因：资源已存在但不�?State �?# 解决：使�?terraform import 导入
terraform import tencentcloud_vpc.main vpc-xxxxxxxx
```

### 6.1.4 依赖错误

```bash
# 错误：循环依�?Error: Cycle: module.vpc.tencentcloud_vpc.main

# 解决：检查资源间的相互引用，消除循环
# 常见场景：安全组 A 引用安全�?B，安全组 B 引用安全�?A
```

### 6.1.5 配额限制

```bash
# 错误：超出配�?Error: Code=LimitExceeded
  LimitExceeded: 您已超出该资源的配额限制

# 常见腾讯云配额限制：
# - 每个 VPC 最�?10 个路由表
# - 每个地域最�?50 个安全组
# - 每个账号最�?500 �?CVM 实例
# - 每个 CLB 最�?50 个监听器

# 解决：在腾讯云控制台申请提高配额
```

### 6.1.6 腾讯云特有错�?
#### 错误 1：可用区实例类型售罄

```bash
Error: Code=InvalidParameterValue
  InvalidParameterValue: 指定的实例类型在当前可用区已售罄

# 原因：该可用区的特定实例类型没有库存
# 解决�?# 1. 更换可用区（�?ap-guangzhou-3 �?ap-guangzhou-6�?# 2. 更换实例类型（如 S5.LARGE8 �?S5.2XLARGE16�?# 3. 使用 Data Source 查询可用实例类型
```

#### 错误 2：镜�?ID 不存�?
```bash
Error: Code=InvalidParameterValue
  InvalidParameterValue: 镜像 ID 不存在或被禁�?
# 原因：指定的镜像 ID 在目标地域不存在
# 解决�?# 1. 确认镜像 ID 在当前地域可�?# 2. 使用 Data Source 动态查询最新镜�?# 3. 不同地域的镜�?ID 不同，需要切换地域查�?```

#### 错误 3：VPC CIDR 冲突

```bash
Error: Code=InvalidParameterValue
  InvalidParameterValue: VPC CIDR 与已�?VPC 冲突

# 原因：VPC CIDR 段与同一地域的其�?VPC 重叠
# 解决：更�?CIDR 段，或使用不同的 VPC
```

#### 错误 4：安全组规则数量超限

```bash
Error: Code=LimitExceeded
  LimitExceeded: 安全组规则数量超过上�?
# 原因：每个安全组最�?100 条规则（入站+出站�?# 解决�?# 1. 合并规则（如�?80 �?443 合并�?80,443�?# 2. 拆分安全�?# 3. 向腾讯云申请提高配额
```

#### 错误 5：COS Bucket 名称已存�?
```bash
Error: Code=BucketAlreadyExists
  BucketAlreadyExists: 请求�?Bucket 名称已被使用

# 原因：COS Bucket 名称全局唯一，已被其他用户使�?# 解决：使用更独特的名称，如添�?app_id 后缀
```

#### 错误 6：资源正在操作中

```bash
Error: Code=ResourceInUse
  ResourceInUse: 资源正在执行其他操作，请稍后重试

# 原因：腾讯云 API 限流或资源正在被其他操作占用
# 解决：等�?30 秒后重试
```

### 6.1.7 Terraform 状态锁定错�?
```bash
Error: Error acquiring the state lock
  Error message: the state file is already locked
  Lock Info:
    ID:        1234567890
    Path:      prod/terraform.tfstate
    Operation: OperationTypeApply
    Who:       user@company.com
    Version:   1.9.0
    Created:   2024-01-01 10:00:00

# 常见原因�?# 1. 其他团队成员正在执行 terraform apply
# 2. 上次操作异常中断，锁未释�?# 3. CI/CD 并发执行

# 正确做法�?# 1. 联系锁持有者确认操作状�?# 2. 如果锁持有者已经崩溃，使用 force-unlock
# 3. 优化 CI/CD 避免并发
```

---

## 6.2 State 操作

### 安全操作原则

```bash
# 1. 修改 State 前先备份
terraform state pull > backup-$(date +%Y%m%d-%H%M%S).tfstate

# 2. 修改 State 前确保有最新版�?terraform state pull > current.tfstate

# 3. 修改后验�?Plan
terraform plan
```

### 常用 State 命令

```bash
# 列出 State 中的资源
terraform state list

# 查看特定资源的属�?terraform state show tencentcloud_cvm_instance.web

# �?State 中移除资源（不删除云资源�?terraform state rm tencentcloud_cvm_instance.web

# 将资源移�?State
terraform import tencentcloud_cvm_instance.web ins-xxxxxxxx

# 移动资源（重命名�?terraform state mv tencentcloud_cvm_instance.old tencentcloud_cvm_instance.new

# 拉取远程 State 到本�?terraform state pull

# 推送本�?State 到远�?terraform state push
```

### 恢复损坏�?State

```bash
# 1. 从备份恢�?terraform state push backup.tfstate

# 2. 如果本地没有备份，从 COS 下载
# 进入 COS 控制台，找到 State 文件
# COS 控制�?�?Bucket 列表 �?对应 Bucket �?文件列表
# 找到 State 文件，下载并检查版本历�?
# 3. 重新导入资源
terraform import tencentcloud_cvm_instance.web ins-xxxxxxxx
```

---

## 6.3 terraform import

### 导入单个资源

```bash
# 1. �?.tf 文件中声明资源骨�?resource "tencentcloud_cvm_instance" "web" {
  # 留空，只要声明类型和名称
}

# 2. 导入
terraform import tencentcloud_cvm_instance.web ins-xxxxxxxx

# 3. 查看导入后的属性（获取所有属性值）
terraform state show tencentcloud_cvm_instance.web

# 4. 将属性复制到 .tf 文件�?# 5. 验证
terraform plan
```

### 使用 import block（Terraform 1.5+�?
```hcl
# �?.tf 文件中声�?import �?import {
  to = tencentcloud_cvm_instance.web
  id = "ins-xxxxxxxx"
}

import {
  to = tencentcloud_vpc.main
  id = "vpc-xxxxxxxx"
}
```

```bash
# 自动生成配置
terraform plan -generate-config-out=generated.tf

# 查看生成的配�?cat generated.tf

# 将生成的配置合并�?main.tf
# 删除 import �?# 执行 apply
terraform apply
```

### 批量导入

```bash
# 使用 PowerShell 脚本批量导入
$resources = @(
  @{type="tencentcloud_vpc"; name="main"; id="vpc-xxxxxxxx"},
  @{type="tencentcloud_subnet"; name="public"; id="subnet-xxxxxxxx"},
  @{type="tencentcloud_cvm_instance"; name="web"; id="ins-xxxxxxxx"}
)

foreach ($r in $resources) {
  terraform import "$($r.type).$($r.name)" $r.id
}
```

### 导入时的注意事项

```
1. 导入前确�?.tf 文件中有对应�?resource 声明
2. 导入后一定执�?terraform plan 验证
3. 导入只是导入 State，不会修改云资源本身
4. 如果 .tf 文件和实际资源配置不一致，plan 会显示变�?5. 谨慎使用 terraform state rm，它会断开 Terraform 对资源的控制
```

---

## 6.4 Drift 检测与修复

### 什么是 Drift（漂移）�?
Drift 是指**实际云资源状�?*�?**Terraform State 记录** 不一致的情况�?
```
Drift 常见原因�?1. 有人通过控制台手动修改了资源（最常见�?2. 自动伸缩组创�?销毁了实例
3. CI/CD 流程中断导致 State 未更�?4. 灾难恢复导致资源重建
5. 运营商自动迁移（如磁盘迁移）
```

### 检�?Drift

```bash
# 刷新 State（从云上拉取最新状态）
terraform refresh

# 对比计划（显示实际与代码的差异）
terraform plan
```

### 修复 Drift

```hcl
# 方案一：更�?.tf 文件匹配实际状�?# 1. 查看实际状�?terraform state show tencentcloud_cvm_instance.web

# 2. 更新 .tf 文件中的属性（�?tags 被手动修改）
# 3. 验证
terraform plan  # 期望输出：No changes

# 方案二：重新导入（如�?.tf 文件已正确）
# 1. �?State 中移�?terraform state rm tencentcloud_cvm_instance.web

# 2. 重新导入
terraform import tencentcloud_cvm_instance.web ins-xxxxxxxx

# 方案三：就地更新（如果期望实际状态被覆盖�?terraform apply  # 这将把资源改�?.tf 中定义的配置
```

### 预防 Drift 的最佳实�?
```
1. 禁止手动修改控制台：所有变更通过 Terraform 进行
2. 使用标签标记：所�?Terraform 管理的资源打�?ManagedBy=terraform 标签
3. 定期检测：CI/CD 中定期执�?terraform plan 检�?Drift
4. 告警通知：Drift 检测到异常时通知团队
5. 自动化修复：计划内的 Drift（如 ASG 自动扩缩）通过 ignore_changes 忽略
```

---

## 6.5 团队协作最佳实�?
### State 锁定

```hcl
# COS 后端自动支持状态锁�?terraform {
  backend "cos" {
    bucket = "terraform-state"
    region = "ap-guangzhou"
    key    = "prod/terraform.tfstate"
  }
}
```

当一个人执行 `terraform apply` 时，State 被锁定，其他人将看到�?
```bash
Error: Error acquiring the state lock

Error message: the state file is already locked
Lock Info:
  ID:        1234567890
  Path:      prod/terraform.tfstate
  Operation: OperationTypeApply
  Who:       user@company.com
  Version:   1.9.0
  Created:   2024-01-01 10:00:00
```

### force-unlock 的安全警�?
> ⚠️ **⚠️ 危险操作：force-unlock ⚠️**
> 
> `terraform force-unlock` �?*强制解除 State 锁定**，但�?> - 如果锁持有者仍在执行操作，强制解锁会导�?State 损坏
> - 两个进程同时写入 State 会导致数据丢�?> - **只有在确认锁持有者已经崩溃或操作已经终止时才能使�?*
> 
> **安全解锁流程�?*
> 1. 联系锁持有者，确认他们已停止操�?> 2. 检�?CI/CD 流水线，确认没有正在运行�?Job
> 3. 执行 `terraform force-unlock <LOCK_ID>`
> 4. 立即执行 `terraform plan` 验证 State 完整�?
```bash
# 强制解锁（仅当确认锁持有者已崩溃�?terraform force-unlock 1234567890

# 如果忘记�?Lock ID，可以从错误信息中获�?# 也可以通过 state pull 查看
terraform state pull | grep -i "lock"
```

### 代码审查清单

```markdown
## Terraform 代码审查清单

### 安全�?- [ ] SecretId/SecretKey 是否通过环境变量传入�?- [ ] 密码是否使用 sensitive 标记�?- [ ] 安全组是否最小权限原则？
- [ ] 是否有不必要的公网暴露？
- [ ] SSH 端口是否限制 IP�?
### 可靠�?- [ ] 是否使用远程 State�?- [ ] 关键资源是否有备份策略？
- [ ] 是否跨可用区部署�?- [ ] 是否有弹性伸缩配置？
- [ ] 是否有健康检查配置？

### 可维护�?- [ ] 是否使用模块化？
- [ ] 资源命名是否规范�?- [ ] 是否有完整标签？
- [ ] 变量是否�?description�?- [ ] 代码是否经过 terraform fmt�?
### 成本
- [ ] 实例类型是否合理�?- [ ] 是否设置了生命周期策略？
- [ ] 非生产环境是否限制资源规格？
- [ ] 是否有未使用的资源？
```

### 团队推荐工作�?
```bash
# 团队推荐工作�?1. git checkout -b feature/add-redis           # 创建功能分支
2. 编写代码                                      # 修改 Terraform 配置
3. terraform fmt && terraform validate          # 格式化和验证
4. terraform plan                               # 确认变更
5. git commit -m "feat: add redis cluster"      # 提交
6. git push origin feature/add-redis            # 推�?7. 创建 PR，等待审�?                             # Code Review
8. 审查通过�?merge �?main                       # 合并
9. CI/CD 自动执行 apply                          # 自动部署
```

---

## 6.6 常用诊断命令

```bash
# 格式化代码（递归所有子目录�?terraform fmt -recursive

# 验证语法
terraform validate

# 查看资源依赖图（需�?Graphviz�?terraform graph | dot -Tpng > graph.png

# 查看详细日志（调试模式）
TF_LOG=DEBUG terraform apply

# 只查看错误日�?TF_LOG=ERROR terraform apply

# 将日志输出到文件
TF_LOG_PATH=terraform.log TF_LOG=DEBUG terraform apply

# 查看特定资源的属�?terraform state show tencentcloud_cvm_instance.web

# 查看版本信息
terraform version

# 查看 Provider 版本
terraform providers

# 查看所有输�?terraform output

# 查看特定输出（JSON 格式�?terraform output -json public_ip

# 查看 resource �?schema
terraform providers schema -json
```

### TF_LOG 日志级别

| 级别 | 说明 | 使用场景 |
|------|------|----------|
| `TRACE` | 最详细的日�?| 深入排查 Provider 内部问题 |
| `DEBUG` | 详细的调试信�?| 日常排查 |
| `INFO` | 信息性消�?| 了解操作流程 |
| `WARN` | 警告信息 | 发现潜在问题 |
| `ERROR` | 只显示错�?| 快速定位错�?|

---

## 6.7 灾难恢复

### State 文件丢失或损�?
```bash
# 场景：terraform.tfstate 被误删，但云资源还在

# 方案一：从 COS 版本历史恢复（推荐）
# COS 控制�?�?找到 State 文件 �?版本管理 �?恢复历史版本

# 方案二：重新导入所有资�?# 1. 列出所有云资源（通过控制台或 CLI�?# 2. 逐个导入�?Terraform
terraform import tencentcloud_vpc.main vpc-xxxxxxxx
terraform import tencentcloud_subnet.public subnet-xxxxxxxx
# ...

# 方案三：使用 terraformer 批量导入
terraformer import tencentcloud --resources=vpc,cvm,clb --regions=ap-guangzhou
```

### Provider 版本不兼�?
```bash
# 场景：升�?Provider 后出现不兼容

# 回退到之前版�?terraform init -reconfigure

# 修改 required_providers 的版本约�?terraform {
  required_providers {
    tencentcloud = {
      source  = "tencentcloudstack/tencentcloud"
      version = "~> 1.80.0"  # 锁定�?1.80.x 版本
    }
  }
}

# 重新初始�?terraform init
```

---

## 🎯 互动练习

### 自测�?
<details>
<summary>📝 �?1 题：看到 `Error: Could not load plugin` 应该怎么办？</summary>

**答案**：运�?`terraform init`。这个错误通常是因为没有初始化工作目录，Provider 插件未下载�?</details>

<details>
<summary>📝 �?2 题：什么是 Drift（漂移）？怎么修复�?/summary>

**答案**：Drift 是实际云资源状态与 Terraform State 记录不一致的情况。修复方式：
1. 如果 .tf 文件是期望状�?�?执行 `terraform apply` 将云资源改回 .tf 定义
2. 如果云资源是期望状�?�?执行 `terraform refresh` 更新 State，或修改 .tf 匹配实际状�?</details>

<details>
<summary>📝 �?3 题：多人协作时，为什么需要远�?State�?/summary>

**答案**：避�?State 文件冲突、覆盖和损坏。远�?State 支持状态锁定（防止并发写入），支持团队协作�?CI/CD 集成�?</details>

### 🖐�?动手试一�?
1. 模拟一�?Drift 场景：在控制台手动修改一个安全组规则，然后运�?`terraform plan` 观察输出
2. 练习 `terraform state list`、`terraform state show`、`terraform state rm` 三个命令
3. 创建一�?COS 远程 State 后端，配�?`terraform init` 并验�?State 已上传到 COS

### 💡 思考题

> 如果 `terraform destroy` 过程中断网了，State 文件和云资源分别处于什么状态？应该怎么恢复�?>
> <details>
> <summary>点击查看答案</summary>
> 断网�?State 可能处于"锁定"状态（lock），部分资源已销毁、部分还在。恢复步骤：
> 1. 如果 State 被锁定，�?`terraform force-unlock <lock_id>` 解锁
> 2. 运行 `terraform plan` 查看当前期望状态和实际状态的差异
> 3. Terraform 会尝试补齐未完成的销毁，或报告已销毁的资源
> 4. 如果 State 已记录资源销毁但云上资源还在，用 `terraform import` 重新导入
> </details>

---

## �?本阶段掌握要�?
- [ ] 能识别和解决常见错误（Provider、认证、资源冲突、配额限制）
- [ ] 了解腾讯云特有的错误信息（售罄、镜像不存在、CIDR 冲突�?- [ ] 掌握 State 操作命令（list、show、rm、mv、pull、push�?- [ ] 会使�?terraform import 导入现有资源
- [ ] 使用 import block（Terraform 1.5+）自动生成配�?- [ ] 能检测和修复 Drift
- [ ] 理解团队协作中的 State 锁定机制
- [ ] 了解 force-unlock 的安全风�?- [ ] 掌握 Terraform 调试技巧（TF_LOG�?- [ ] 了解灾难恢复方案
- [ ] 能使用代码审查清单确保代码质�?
---

**下一�?�?[07-项目实战](07-项目实战.md)**