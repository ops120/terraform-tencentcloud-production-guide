# Phase 5：生产环境实�?
> 预计用时�?~7 �? 
> 目标：掌握生产级 Terraform 项目标准、安全策略、CI/CD 集成和架构模�?
---

## 5.1 生产环境目录结构

### 推荐目录布局

```
terraform-infra/
├── environments/                  # 环境目录
�?  ├── dev/
�?  �?  ├── main.tf               # 主配�?�?  �?  ├── backend.tf            # 后端配置
�?  �?  ├── provider.tf           # Provider 配置
�?  �?  ├── variables.tf          # 变量定义
�?  �?  ├── terraform.tfvars      # 变量值（不入 Git�?�?  �?  └── outputs.tf            # 输出
�?  ├── staging/
�?  �?  ├── main.tf
�?  �?  ├── backend.tf
�?  �?  ├── ...
�?  �?  └── outputs.tf
�?  └── prod/
�?      ├── main.tf
�?      ├── backend.tf
�?      ├── ...
�?      └── outputs.tf
�?├── modules/                       # 本地模块
�?  ├── vpc/
�?  ├── cvm/
�?  ├── mysql/
�?  ├── redis/
�?  ├── clb/
�?  ├── cos/
�?  └── security_group/
�?├── scripts/                       # 辅助脚本
�?  ├── init-backend.sh           # 初始化后端存�?�?  └── plan-all.sh               # 批量执行 plan
�?├── .gitignore                     # Git 忽略规则
└── README.md                      # 项目说明
```

### 为什么这样设计？

- **环境隔离**：每个环境有独立的目录、State 文件、变量值和权限控制
- **模块共享**：`modules/` 目录下的模块被所有环境共�?- **最小变更范�?*：修�?prod 的配置不会影�?dev
- **权限控制**：Git 分支权限 + CI/CD 权限，dev 开发者可操作，prod 需要审�?
### .gitignore 配置

```gitignore
# 本地 State 文件
*.tfstate
*.tfstate.backup
*.tfstate.lock.info

# 变量值文件（含敏感信息）
terraform.tfvars
*.auto.tfvars

# 覆盖文件
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI 配置文件
.terraform.tfstate.lock.info
.terraformrc
terraform.rc

# 插件目录
.terraform/

# 临时文件
*.log
crash.log

# 操作系统文件
Thumbs.db
.DS_Store
```

### 环境隔离原则

```
┌─────────────────────────────────────────────�?�?             Git 仓库                        �?�? terraform-infra/                            �?�?   ├── environments/dev/     �?开发者可操作  �?�?   ├── environments/staging/ �?�?CI 可操�? �?�?   └── environments/prod/    �?�?CI + 审批  �?└─────────────────────────────────────────────�?```

每个环境目录是一个独立的 Terraform 工作目录，有独立的：
- State 文件（COS 不同 prefix�?- Provider 配置
- 变量�?- 权限控制

---

## 5.2 Secret 管理

### 原则

```
�?不要做：
  - �?.tf 文件中硬编码密码
  - �?terraform.tfvars 提交�?Git
  - �?CI 日志中输出敏感变�?
�?应该做：
  - 使用环境变量（TF_VAR_*�?  - 使用密钥管理服务（SSM / KMS�?  - 使用 CI/CD �?Secret 变量
```

### 方式一：环境变量（推荐�?
```bash
# 设置腾讯云认证（Provider 自动读取�?export TENCENTCLOUD_SECRET_ID="your_secret_id"
export TENCENTCLOUD_SECRET_KEY="your_secret_key"

# 设置自定义变量（Terraform 自动读取 TF_VAR_ 前缀的环境变量）
export TF_VAR_db_password="complex_password"
export TF_VAR_redis_password="complex_redis_password"
export TF_VAR_admin_ip="your_ip/32"
```

**�?CI/CD 中传入环境变量：**

```bash
# Terraform 会自动读�?TF_VAR_ 前缀的环境变�?# 所�?CI/CD 中只需要设置环境变量即可，无需额外参数
terraform plan
terraform apply -auto-approve
```

### 方式二：使用腾讯�?SSM（参数存储）

腾讯�?SSM（参数存储服务）可以安全地存储密钥和配置信息�?
```hcl
# 查询 SSM 中的密钥（返回密钥列表）
data "tencentcloud_ssm_secrets" "all" {
  secret_name = "prod-db-password"  # �?SSM 控制台预先创�?}

# 获取密钥的最新版�?data "tencentcloud_ssm_secret_versions" "db_password" {
  secret_name = "prod-db-password"
  version_id  = "latest"
}

locals {
  db_password = try(data.tencentcloud_ssm_secret_versions.db_password.version_value, "")
}

resource "tencentcloud_mysql_account" "app" {
  password = local.db_password
}
```

**�?SSM 中创建密钥：**

```bash
# 通过腾讯�?CLI 创建密钥
tccli ssm CreateSecret --SecretName "prod-db-password" --VersionId "v1" --SecretString "MySecureP@ss123!"
```

### 方式三：腾讯�?KMS 加密

```hcl
# 使用 KMS 加密的敏感信息（需要先创建 KMS 密钥�?data "tencentcloud_kms_keys" "default" {
  key_state = 1  # Enabled
}

# 对密码进行加�?# 加密后的密文可以在代码中安全使用
```

### 方式四：CI/CD �?Secret 变量

```yaml
# GitHub Actions 示例
env:
  TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_CLOUD_SECRET_ID }}
  TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_CLOUD_SECRET_KEY }}
  TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
  TF_VAR_redis_password: ${{ secrets.REDIS_PASSWORD }}
```

---

### 方式五：CAM 子用�?+ 最小权限（推荐生产环境�?
生产环境不应该使用主账号�?API 密钥，而是创建**专用子用�?*并授予最小权限：

```hcl
# 查询当前账号信息（获�?UIN�?data "tencentcloud_user_info" "current" {}

# 创建 CAM 子用户（仅用�?Terraform 操作�?resource "tencentcloud_cam_user" "terraform" {
  name                = "terraform-deployer"
  remark              = "Terraform 部署专用账号"
  console_login       = false        # 禁止控制台登�?  use_api             = true         # 允许 API 访问
  need_reset_password = false
}

# 生成 API 密钥（SecretId + SecretKey�?resource "tencentcloud_cam_access_key" "terraform" {
  target_uin = data.tencentcloud_user_info.current.uin
  status     = "Active"
  # 注意：SecretKey 只能在此处获取一次，apply �?State 中明文存�?  # 建议 apply 后立即将 SecretKey 保存�?SSM 或环境变量中
}

# 关联策略：Terraform 全读写权限（腾讯云提供了预置策略�?resource "tencentcloud_cam_user_policy_attachment" "terraform_full" {
  user_id   = tencentcloud_cam_user.terraform.id
  policy_id = "1"  # 1 = AdministratorAccess（全读写权限�?  # 生产环境建议使用自定义策略，仅授予必要的资源权限
}

# 自定义策略示例（仅允许操�?VPC �?CVM�?resource "tencentcloud_cam_policy" "terraform_custom" {
  name        = "terraform-deploy-policy"
  document    = jsonencode({
    version = "2.0"
    statement = [
      {
        effect    = "allow"
        action    = [
          "vpc:*",
          "cvm:*",
          "clb:*",
          "mysql:*",
          "redis:*",
          "cos:*"
        ]
        resource  = ["*"]
      }
    ]
  })
  description = "Terraform 部署所需的最小权�?
}
```

> ⚠️ **CAM 子用户注意事�?*�?> - 子用户的 SecretKey 在创建后**只显示一�?*，务必及时保�?> - 建议为每个环境（dev/staging/prod）创建独立的子用�?> - 生产环境使用**自定义策�?*而非 `AdministratorAccess`
> - 定期轮换密钥（腾讯云推荐�?90 天更换一次）

## 5.3 CI/CD 集成

### GitHub Actions 示例

```yaml
# .github/workflows/terraform.yml
name: Terraform

on:
  push:
    branches: [main]
    paths:
      - 'environments/prod/**'
  pull_request:
    branches: [main]
    paths:
      - 'environments/prod/**'

env:
  TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_CLOUD_SECRET_ID }}
  TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_CLOUD_SECRET_KEY }}
  TF_VERSION: '1.9.0'

jobs:
  terraform:
    name: Terraform Plan/Apply
    runs-on: ubuntu-latest
    
    defaults:
      run:
        # working-directory 指定 Terraform 命令执行的目�?        # 路径相对于仓库根目录
        working-directory: ./environments/prod

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      - name: Terraform Init
        run: terraform init

      - name: Terraform Format
        run: terraform fmt -check

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        id: plan
        run: |
          terraform plan -no-color
        # plan 的输出会显示�?GitHub Actions 日志�?
      # 使用 GitHub Actions �?Step Summary 输出 Plan 结果
      - name: Update Summary
        run: |
          echo "## Terraform Plan Result" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.plan.outputs.stdout }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

      # �?main 分支�?push 事件执行 apply
      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: terraform apply -auto-approve
        env:
          TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
          TF_VAR_redis_password: ${{ secrets.REDIS_PASSWORD }}
```

### GitLab CI 示例

```yaml
# .gitlab-ci.yml
image: hashicorp/terraform:1.9

cache:
  key: "${CI_COMMIT_REF_SLUG}"
  paths:
    - .terraform

before_script:
  - cd environments/prod
  - terraform init

stages:
  - validate
  - plan
  - apply

variables:
  TENCENTCLOUD_SECRET_ID: ${TENCENT_CLOUD_SECRET_ID}   # GitLab CI/CD Variables
  TENCENTCLOUD_SECRET_KEY: ${TENCENT_CLOUD_SECRET_KEY}

validate:
  stage: validate
  script:
    - terraform validate
    - terraform fmt -check

plan:
  stage: plan
  script:
    - terraform plan -out=plan.out    # 输出到文�?  artifacts:
    name: "terraform-plan-${CI_COMMIT_REF_SLUG}"
    paths:
      - environments/prod/plan.out    # 保存 plan 文件�?apply 阶段使用
    expire_in: 1 day                  # 一天后过期

apply:
  stage: apply
  script:
    - terraform apply plan.out        # 使用 plan 文件
  environment:
    name: production
  only:
    - main
  when: manual                         # 需要手动审批（GitLab 手动触发�?  dependencies:
    - plan                             # 依赖 plan 阶段�?artifacts
```

### CI/CD 工作�?
```
开发者提�?PR
    �?PR 触发 Plan（自动，�?PR 评论中展示结果）
    �?人工审查 Plan 输出（确认没有意外的删除或变更）
    �?Merge �?main 分支
    �?Push 触发 Apply（自动或手动审批�?    �?Terraform 执行变更
    �?通知团队变更结果（Slack / 邮件 / 企业微信�?```

### 环境变量传入 terraform apply 的完整示�?
�?CI/CD 中，通过环境变量传入敏感信息的最佳实践：

```yaml
# GitHub Actions
- name: Terraform Apply
  run: terraform apply -auto-approve
  env:
    # 腾讯云认�?    TENCENTCLOUD_SECRET_ID: ${{ secrets.TENCENT_CLOUD_SECRET_ID }}
    TENCENTCLOUD_SECRET_KEY: ${{ secrets.TENCENT_CLOUD_SECRET_KEY }}
    # Terraform 变量（TF_VAR_ 前缀�?    TF_VAR_db_password: ${{ secrets.DB_PASSWORD }}
    TF_VAR_redis_password: ${{ secrets.REDIS_PASSWORD }}
    TF_VAR_admin_ip: ${{ secrets.ADMIN_IP }}
    # 非敏感变量可以直接写在代码中
```

```bash
# 本地开发时等效命令
export TENCENTCLOUD_SECRET_ID="your_secret_id"
export TENCENTCLOUD_SECRET_KEY="your_secret_key"
export TF_VAR_db_password="complex_password"
export TF_VAR_redis_password="complex_redis_password"
terraform apply -auto-approve
```

---

## 5.4 生产架构模板

### 高可�?Web 应用架构

```
                        ┌───────────────�?                        �?  DNS 解析     �?                        �? dnspod.record �?                        └───────┬───────�?                                �?                        ┌───────▼───────�?                        �? CLB 负载均衡  �?                        �? (HTTPS:443)  �?                        └───────┬───────�?                                �?          ┌─────────────────────┼─────────────────────�?          �?                    �?                    �?  ┌───────▼───────�?   ┌───────▼───────�?   ┌───────▼───────�?  �? CVM Web-01   �?   �? CVM Web-02   �?   �? CVM Web-03   �?  �? ap-guangzhou-3�?   �? ap-guangzhou-3�?   �? ap-guangzhou-4�?  └───────┬───────�?   └───────┬───────�?   └───────┬───────�?          �?                    �?                    �?          └─────────────────────┼─────────────────────�?                                �?          ┌─────────────────────┼─────────────────────�?          �?                    �?                    �?  ┌───────▼───────�?   ┌───────▼───────�?   ┌───────▼───────�?  �? MySQL 主库    �?   �? MySQL 只读    �?   �? Redis 缓存    �?  �? ap-guangzhou-3�?   �? ap-guangzhou-4�?   �? ap-guangzhou-3�?  �? 内网子网      �?   �? 内网子网      �?   �? 内网子网      �?  └───────────────�?   └───────────────�?   └───────────────�?          �?  ┌───────▼───────�?  �? COS 对象存储    �?  �? 静态资�?备份   �?  └───────────────�?```

### 生产环境目录结构

```hcl
terraform-infra/environments/prod/
├── main.tf           # 主入口，调用模块
├── backend.tf        # COS 后端配置
├── provider.tf       # Provider 配置
├── variables.tf      # 变量定义
├── terraform.tfvars  # 实际值（不入 Git�?├── outputs.tf        # 输出
└── locals.tf         # 本地变量和标�?```

### main.tf 示例

```hcl
# locals.tf
locals {
  project     = "myapp"
  environment = "production"
  region      = "ap-guangzhou"
  azs         = ["ap-guangzhou-3", "ap-guangzhou-4"]
  
  common_tags = {
    Project     = local.project
    Environment = local.environment
    ManagedBy   = "terraform"
    Owner       = "platform-team"
  }
}

# main.tf
module "vpc" {
  source = "../../modules/vpc"

  vpc_name            = "${local.project}-${local.environment}"
  vpc_cidr            = "10.0.0.0/16"
  public_subnet_cidrs  = ["10.0.1.0/24", "10.0.3.0/24"]
  private_subnet_cidrs = ["10.0.2.0/24", "10.0.4.0/24"]
  availability_zones   = local.azs
  tags                 = local.common_tags
}

module "sg" {
  source = "../../modules/security_group"

  project     = local.project
  environment = local.environment
  vpc_id      = module.vpc.vpc_id
  admin_ip    = var.admin_ip
  tags        = local.common_tags
}

module "cvm" {
  source = "../../modules/cvm"

  instance_name      = "${local.project}-web"
  instance_count     = 3
  instance_type      = "S5.4XLARGE32"
  image_id           = data.tencentcloud_images.default.images[0].image_id
  vpc_id             = module.vpc.vpc_id
  subnet_ids         = module.vpc.public_subnet_ids
  security_group_ids = [module.sg.web_sg_id]
  key_ids            = [tencentcloud_key_pair.main.id]
  user_data          = file("${path.module}/scripts/init.sh")
  tags               = local.common_tags
}

module "clb" {
  source = "../../modules/clb"

  clb_name     = "${local.project}-${local.environment}"
  vpc_id       = module.vpc.vpc_id
  subnet_id    = module.vpc.public_subnet_ids[0]
  instance_ids = module.cvm.instance_ids
  ssl_cert_id  = data.tencentcloud_ssl_certificates.cert.certificates[0].id
  tags         = local.common_tags
}

module "mysql" {
  source = "../../modules/mysql"

  instance_name     = "${local.project}-${local.environment}"
  mem_size          = 16000
  volume_size       = 500
  vpc_id            = module.vpc.vpc_id
  subnet_id         = module.vpc.private_subnet_ids[0]
  availability_zone = local.azs[0]
  security_group_id = module.sg.mysql_sg_id
  db_password       = var.db_password
  tags              = local.common_tags
}

module "redis" {
  source = "../../modules/redis"

  instance_name     = "${local.project}-${local.environment}"
  mem_size          = 8192
  vpc_id            = module.vpc.vpc_id
  subnet_id         = module.vpc.private_subnet_ids[0]
  availability_zone = local.azs[0]
  security_group_id = module.sg.redis_sg_id
  redis_password    = var.redis_password
  tags              = local.common_tags
}

module "cos" {
  source = "../../modules/cos"

  bucket_name = "${local.project}-${local.environment}-${var.app_id}"
  tags        = local.common_tags
}
```

---

## 5.5 从控制台迁移�?Terraform

### 迁移策略

```
1. 识别 �?列出所有需要管理的资源
2. 导入 �?将现有资源导�?Terraform State
3. 编写 �?编写匹配�?.tf 配置文件
4. 验证 �?terraform plan 确认无差�?5. 接管 �?开始用 Terraform 管理
```

### terraform import 命令

```bash
# 语法：terraform import <资源类型>.<资源�? <资源ID>
terraform import tencentcloud_vpc.main vpc-xxxxxxxx
terraform import tencentcloud_cvm_instance.web ins-xxxxxxxx
```

### 使用 import block（Terraform 1.5+�?
```hcl
# 在代码中声明要导入的资源
import {
  to = tencentcloud_vpc.main
  id = "vpc-xxxxxxxx"
}

import {
  to = tencentcloud_cvm_instance.web
  id = "ins-xxxxxxxx"
}
```

```bash
# 执行导入（Terraform 1.5+ 自动生成配置�?terraform plan -generate-config-out=generated.tf

# 检查生成的配置
cat generated.tf

# 将生成的配置复制�?main.tf 中，调整后执�?terraform apply
```

### 批量迁移建议

```
1. 先迁移网络层（VPC、子网、路由表、安全组�?2. 再迁移存储层（COS、CBS、MySQL、Redis�?3. 最后迁移计算层（CVM、CLB、弹性伸缩）
4. 每次迁移后验�?plan 输出�?"No changes"
```

### 使用 terraformer 批量导入

[terraformer](https://github.com/GoogleCloudPlatform/terraformer) 是一个开源工具，可以批量导入云资源并生成 Terraform 配置文件�?
```bash
# 安装 terraformer
# 下载对应平台的二进制文件

# 批量导入腾讯云资�?terraformer import tencentcloud --resources=vpc,cvm,clb --regions=ap-guangzhou

# 导入结果�?generated/ 目录�?```

---

## 5.6 Terraform 策略与合�?
### 使用 check 块（Terraform 1.5+�?
```hcl
# 在顶级定�?data �?data "terraform_remote_state" "current" {
  backend = "cos"
  config = {
    bucket = "company-terraform-state"
    region = "ap-guangzhou"
    key    = "prod/terraform.tfstate"
  }
}

# 检查资源是否合�?check "cost_check" {
  assert {
    condition     = alltrue([
      for inst in tencentcloud_cvm_instance.web : inst.instance_type != "S5.8XLARGE64"
    ])
    error_message = "生产环境不允许使�?32C64G 超大实例�?
  }
}
```

### 命名规范

```hcl
# 资源命名规范
resource "tencentcloud_cvm_instance" "web" {      # 简短有意义
  instance_name = "myapp-prod-web-01"             # project-env-role-序号
  tags = {
    Name        = "myapp-prod-web-01"
    Project     = "myapp"
    Environment = "production"
    Role        = "web"
    ManagedBy   = "terraform"
  }
}
```

### 标签策略

```hcl
locals {
  required_tags = {
    Project     = "myapp"
    Environment = "production"
    ManagedBy   = "terraform"
    Owner       = "platform-team"
    CostCenter  = "infra-123"
  }
}

# 检查标签是否完�?check "tag_check" {
  assert {
    condition     = length(tencentcloud_cvm_instance.web) > 0 ? alltrue([
      for k, v in local.required_tags :
      contains(keys(tencentcloud_cvm_instance.web[0].tags), k)
    ]) : true
    error_message = "缺少必要的标签！"
  }
}
```

### 腾讯�?TIC（Tencent Infrastructure Automation）�?CLI 的替代方�?
腾讯云提供了 **TIC（Tencent Infrastructure Automation�?* 产品，可以在腾讯云控制台�?*可视化地管理 Terraform 模板和部�?*，适合不熟�?CLI 或需要图形化管理的团队：

| 功能 | TIC | CLI（本教程推荐�?|
|------|-----|-----------------|
| 操作方式 | 控制台可视化 + 代码 | 纯命令行 |
| 模板管理 | 内置模板市场 | 自己维护 |
| 部署历史 | 自动记录 | 需配合 CI/CD |
| 适合场景 | 运维团队、可视化需�?| DevOps、自动化流水�?|
| 学习成本 | �?| �?|

**TIC 的核心能力：**

- **模板市场**：预置的 Terraform 模板（WordPress、LNMP 等），一键部�?- **可视化编�?*：拖拽式资源编排，自动生�?Terraform 代码
- **部署管理**：版本管理和部署历史，支�?Plan/Apply/Destroy
- **合规检�?*：内置合规规则，检查资源是否符合规�?
**使用方式�?*

```bash
# 通过控制台操�?# 登录腾讯云控制台 �?云资源自动化 for Terraform �?创建资源�?# 支持：上传模�?/ 可视化编�?/ 选择模板市场模板
# 支持：Plan（预览）�?Apply（执行）�?Destroy（销毁）完整流程
```

> 🔗 **TIC 官方链接**：https://cloud.tencent.com/product/tic  
> 📖 **TIC 文档**：https://cloud.tencent.com/document/product/1653

> 💡 **建议**：学习阶段使�?CLI 方式（本教程推荐），能更好理�?Terraform 的工作原理。团队协作时可以考虑 TIC 作为补充，尤其适合需要可视化审批流程的运维团队�?
### 💡 思考题

> 如果 CI/CD �?`terraform plan` 通过了但 `terraform apply` 失败了（比如网络超时），State 文件会处于什么状态？应该怎么恢复�?>
> <details>
> <summary>点击查看答案</summary>
> 如果 apply 过程中断，State 文件可能处于"部分更新"状态——有些资源已创建，有些没创建。Terraform �?State 锁定机制会阻止再次操作。解决步骤：
> 1. 先检查实际云上哪些资源已创建
> 2. 如果锁定未释放，�?`terraform force-unlock` 解锁
> 3. 再次运行 `terraform plan` �?`terraform apply`，Terraform 会自动补齐未完成的变�?> </details>

---

## 5.7 生产环境 Checklist

### 部署前检查清�?
```markdown
## 生产环境部署检查清�?
### 安全�?- [ ] SecretId/SecretKey 通过环境变量传入，非硬编�?- [ ] 密码使用 sensitive 标记
- [ ] 安全组遵循最小权限原�?- [ ] SSH 端口限制为管�?IP（非 0.0.0.0/0�?- [ ] 数据库在内网子网，不暴露公网
- [ ] 密钥对替代密码登�?
### 可靠�?- [ ] 使用远程 State（COS 后端�?- [ ] 跨可用区部署
- [ ] 关键资源有备份策略（MySQL 备份、CBS 快照�?- [ ] 有弹性伸缩配�?- [ ] 负载均衡健康检查配置正�?
### 可维护�?- [ ] 使用模块�?- [ ] 资源命名规范（project-env-role-序号�?- [ ] 完整标签（Project/Environment/ManagedBy/Owner�?- [ ] 变量�?description
- [ ] 代码经过 terraform fmt �?terraform validate

### 成本
- [ ] 实例类型合理（不过度配置�?- [ ] 非生产环境限制资源规�?- [ ] 设置了生命周期策略（日志自动清理�?- [ ] 非工作时间可关闭 dev 环境
```

---

## 🎯 互动练习

### 自测�?
<details>
<summary>📝 �?1 题：生产环境推荐的目录结构是什么样的？</summary>

**答案**：按环境隔离（`environments/dev/staging/prod`），每个环境有独立的 `main.tf`、`backend.tf`、`terraform.tfvars`，共享的 `modules/` 目录放通用模块�?</details>

<details>
<summary>📝 �?2 题：Secret 管理的首要原则是什么？</summary>

A) 写在 `.tf` 文件中方便管�? 
B) 永远不要硬编码，通过环境变量或密钥管理服务传�? 
C) 写在 README 里方便团队查�? 
D) �?`terraform.tfvars` 管理并提交到 Git

**答案：B**。Secret 管理的核心原则是"敏感信息不入代码"�?</details>

<details>
<summary>📝 �?3 题：`terraform import` 的作用是什么？</summary>

**答案**：将已存在的云资源导�?Terraform State，让 Terraform 接管管理。常用于迁移现有基础设施�?Terraform�?</details>

### 🖐�?动手试一�?
1. 创建一个生产环境目录结构（`environments/prod/`），按照 5.1 节的模板创建所有文�?2. 配置 GitHub Actions 流水线，跑�?`init �?plan �?apply` 流程
3. 在控制台找到一台现有的 CVM �?VPC，用 `terraform import` 导入�?State �?
---

## �?本阶段掌握要�?
- [ ] 掌握生产级目录结构和环境隔离
- [ ] 配置完整 .gitignore
- [ ] 使用环境变量管理 Secret（TF_VAR_* 前缀�?- [ ] 使用腾讯�?SSM 管理密钥
- [ ] 配置 GitHub Actions 流水线（�?working-directory�?- [ ] 配置 GitLab CI 流水线（�?plan.out �?artifacts�?- [ ] 理解 CI/CD 工作流（Plan �?审批 �?Apply�?- [ ] 掌握生产架构模板（高可用 Web�?- [ ] 掌握 `terraform import` 迁移现有资源
- [ ] 使用 import block 批量导入
- [ ] 制定资源命名和标签规�?- [ ] 实施合规检查策�?- [ ] 使用生产环境部署检查清�?
---

**下一�?�?[06-运维与排错](../06-运维与排�?README.md)**